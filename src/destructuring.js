// // Destructuring

// // Object destructuring:

// const smoothie = { fats: 'avocado', fruits: 'blueberry', greens: 'spinach' };

// const { fats, fruits } = smoothie;

// console.log(fats, fruits); // 'spinach'

// prop-drilling
// state

// Event -> state changes -> components(re-render) -> UI(changes)

// state is assigned to a functional component in react using 'State hooks'
// useState or useReducer
